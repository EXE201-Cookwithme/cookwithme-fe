export enum PostStatus {
  PENDING = "pending",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
}

export enum UserRole {
  ADMIN = "admin",
  USER = "user",
}

export enum UserPlan {
  FREE = "free",
  PRO = "pro",
}


